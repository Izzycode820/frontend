query Domains($workspaceId: ID!) {
  domains(workspaceId: $workspaceId) {
    id
    domain
    type
    status
    isPrimary
    managedBy
    addedAt
    subdomainChangesRemaining
    subdomainChangesLimit
  }
}
