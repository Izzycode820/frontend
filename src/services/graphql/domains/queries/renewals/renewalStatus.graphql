query RenewalStatus($renewalId: ID!) {
  renewalStatus(renewalId: $renewalId) {
    id
    domainName
    status
    renewalPriceFcfa
    paymentStatus
    previousExpiryDate
    newExpiryDate
    renewedAt
    errorMessage
    createdAt
  }
}
