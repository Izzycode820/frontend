mutation VerifyCustomDomain($domainId: ID!) {
  verifyCustomDomain(domainId: $domainId) {
    success
    message
    error
    verified
    domain {
      id
      domain
      status
      verifiedAt
    }
  }
}
