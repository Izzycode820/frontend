query GetWorkmanSettings {
  workmanSettings {
    id
    llmProvider
    autoReplyEnabled
    newCustomerHandler
    personas
    brainConfig
    minConfidence
    primaryLanguage
    supportedLanguages
    autoAdaptLanguage
    operatingHours
  }
}
