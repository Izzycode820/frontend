mutation TogglePaymentMethod($methodId: ID!, $enabled: Boolean!) {
  togglePaymentMethod(methodId: $methodId, enabled: $enabled) {
    success
    message
    error
    paymentMethod {
      id
      enabled
    }
  }
}
