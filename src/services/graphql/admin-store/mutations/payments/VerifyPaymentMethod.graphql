mutation VerifyPaymentMethod($methodId: ID!) {
  verifyPaymentMethod(methodId: $methodId) {
    success
    message
    error
    paymentMethod {
      id
      verified
      credentialVerifiedAt
    }
  }
}
