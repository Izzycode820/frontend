mutation UnarchiveOrder($orderId: String!) {
  unarchiveOrder(orderId: $orderId) {
    success
    order {
      id
      orderNumber
      status
      isArchived
      archivedAt
      canBeArchived
      canBeUnarchived
      updatedAt
    }
    message
    error
  }
}
