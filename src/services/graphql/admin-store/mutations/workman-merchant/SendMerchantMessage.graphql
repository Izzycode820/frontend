mutation SendMerchantMessage($sessionId: String, $message: String!) {
  sendMerchantMessage(sessionId: $sessionId, message: $message) {
    success
    sessionId
    errorCode
    responseMessage
  }
}
