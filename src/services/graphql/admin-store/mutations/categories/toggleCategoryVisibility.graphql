mutation ToggleCategoryVisibility(
  $id: ID!
) {
  toggleCategoryVisibility(
    id: $id
  ) {
    success
    category {
      id
      name
      isVisible
    }
    isVisible
    message
    error
  }
}