/**
 * Core Route Constants
 * Base route constants used across the application
 */

// Public routes
export const PUBLIC_ROUTES = {
  HOME: '/',
  CAMP: '/camp',
} as const

// Protected routes
export const PROTECTED_ROUTES = {
  BILLING: '/billing',
} as const